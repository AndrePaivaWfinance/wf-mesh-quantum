{
  "name": "wf-operacao-controlle-ops",
  "version": "1.0.0",
  "description": "Operacoes de integracao com Controlle",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run build",
    "start": "func start --port 7074",
    "test": "jest"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/storage-queue": "^12.16.0",
    "@azure/identity": "^4.0.0",
    "@azure/keyvault-secrets": "^4.7.0"
  },
  "devDependencies": {
    "@azure/functions": "^4.0.0",
    "@types/node": "^20.x",
    "rimraf": "^5.0.0",
    "typescript": "^5.3.0"
  }
}
